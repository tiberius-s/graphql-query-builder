{
  "name": "graphql-query-builder",
  "version": "1.0.0",
  "description": "A GraphQL utility package for building optimized upstream queries based on client field selections. Prevents server-side overfetching in Apollo Federation subgraphs.",
  "author": "Tiberius Silivestru <tiberius.silivestru@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "build:ci": "tsup && attw --pack . && publint",
    "dev": "tsup --watch",
    "test": "vitest run src/",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:ci": "vitest run test/",
    "lint": "biome lint src",
    "lint:fix": "biome lint --write src",
    "format": "biome check --write src",
    "format:check": "biome check src",
    "check": "biome check src",
    "check:fix": "biome check --write src",
    "typecheck": "tsc --noEmit",
    "validate": "npm run build && attw --pack . && publint",
    "prepublishOnly": "npm run validate"
  },
  "keywords": [
    "graphql",
    "apollo",
    "federation",
    "query-builder",
    "overfetching",
    "optimization",
    "datasource",
    "subgraph"
  ],
  "peerDependencies": {
    "graphql": "^16.0.0"
  },
  "dependencies": {
    "graphql-parse-resolve-info": "^4.14.1"
  },
  "devDependencies": {
    "@apollo/subgraph": "^2.12.2",
    "@arethetypeswrong/cli": "^0.18.2",
    "@biomejs/biome": "^2.3.11",
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^4.0.17",
    "graphql": "^16.8.1",
    "graphql-tag": "^2.12.6",
    "publint": "^0.3.16",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17",
    "zod": "^4.3.5"
  }
}
